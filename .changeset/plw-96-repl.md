---
"veto-sdk": minor
---

Add an interactive REPL for `npx veto` with natural-language policy generation, local `/test` and simulation flows, rule explain/export/load/clear commands, and persistent shell history.
