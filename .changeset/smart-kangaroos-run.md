---
"veto": patch
"veto-cli": patch
"veto-sdk": patch
---

Add the canonical `veto` npm CLI package for `npx veto init`, keep existing CLI bins as compatibility paths, and update public local-only docs and reviewer compose defaults.
