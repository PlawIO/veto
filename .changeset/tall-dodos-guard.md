---
"@veto/python-release": minor
---

Sync the Python SDK with the TypeScript SDK's provider adapters, feed-backed rule helpers, economic authorization, model validation, extractors, CLI, and parity docs.
