---
"veto-bash": minor
---

Re-scope `veto-bash` to a Rust-first guarded bash runtime with local deterministic policy evaluation, SWR cloud policy cache refresh, approval polling, audit spooling, and MCP `serve` / `init` support.
